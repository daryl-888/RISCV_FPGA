# Pipeline implementation workspace

Add your five-stage CPU here during weeks 4–5. Preserve the single-cycle CPU as a reference. Start with independent instructions, then forwarding, then load-use stalls, then redirect/flush, then fault ordering. Give every pipeline slot a valid bit.

Reuse the same programs and architecture contract. Compare ordered instruction results, not cycle-by-cycle register snapshots. See [verification](../../docs/VERIFICATION.md).
